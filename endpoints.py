### Login

# * Endpoint path: /login
# * Endpoint method: POST
# * Query parameters:
#   * «name»: «purpose»

# * Headers:
#   * Authorization: Bearer token

# * Request shape (JSON):
#     ```json
#     «JSON-looking thing that has the
#     keys and types in it»
#     ```

# * Response: A login form

# * Response shape (JSON):
#     ```json
#     «JSON-looking thing that has the
#     keys and types in it»
#     ```
